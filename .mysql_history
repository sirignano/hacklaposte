SHOW DATABASES;
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
email VARCHAR(20),
encrypted_password VARCHAR(100),
firstname VARCHAR(20),
lastname VARCHAR(20),
money INT UNSIGNED,
address VARCHAR(50)
)
;
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,  email VARCHAR(20), encrypted_password VARCHAR(100), firstname VARCHAR(20), lastname VARCHAR(20), money INT UNSIGNED, address VARCHAR(50) );
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
SHOW DATABASES;
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
email VARCHAR(20),
encrypted_password VARCHAR(100),
firstname VARCHAR(20),
lastname VARCHAR(20),
money INT UNSIGNED,
address VARCHAR(50)
CREATE DATABASE GroomAddict;
using GroomAddict;
use GroomAddict;
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
email VARCHAR(20),
encrypted_password VARCHAR(100),
firstname VARCHAR(20),
lastname VARCHAR(20),
money INT UNSIGNED,
address VARCHAR(50)
)
;
CREATE TABLE user (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,  email VARCHAR(20), encrypted_password VARCHAR(100), firstname VARCHAR(20), lastname VARCHAR(20), money INT UNSIGNED, address VARCHAR(50) );
INSERT INTO employees (id, email, encrypted_password, firstname, lastname, money, address) VALUES
(1, 'l.sirignano@groupeonepoint.com', '42', 'Lucca', 'Sirignano', 42, '22 avenue du maine'),
(2, 'a.chazal@groupeonepoint.com', '42', 'Arthur', 'Chazal', 21, '41 rue Jussieu');
INSERT INTO employees (id, email, encrypted_password, firstname, lastname, money, address) VALUES (1, 'l.sirignano@groupeonepoint.com', '42', 'Lucca', 'Sirignano', 42, '22 avenue du maine'), (2, 'a.chazal@groupeonepoint.com', '42', 'Arthur', 'Chazal', 21, '41 rue Jussieu');
RENAME TABLE `user` TO `users`;
INSERT INTO users (id, email, encrypted_password, firstname, lastname, money, address) VALUES
(1, 'l.sirignano@groupeonepoint.com', '42', 'Lucca', 'Sirignano', 42, '22 avenue du maine'),
(2, 'a.chazal@groupeonepoint.com', '42', 'Arthur', 'Chazal', 21, '41 rue Jussieu');
INSERT INTO users (id, email, encrypted_password, firstname, lastname, money, address) VALUES (1, 'l.sirignano@groupeonepoint.com', '42', 'Lucca', 'Sirignano', 42, '22 avenue du maine'), (2, 'a.chazal@groupeonepoint.com', '42', 'Arthur', 'Chazal', 21, '41 rue Jussieu');
show users
;
help
use GroomAddict;
SELECT * from users;
SELECT * from users
